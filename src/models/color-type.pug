include ../components/colors/colors
include ../components/fontText/fontText

mixin color-type
    section.color-type
        .color-type__wrapper
            +colors(options)
            +fontText(options)